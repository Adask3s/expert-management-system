spring:
  application:
    name: expert-management-system

  datasource:
    url: jdbc:postgresql://localhost:5432/expert_db
    username: admin
    password: password
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true

  liquibase:
    change-log: classpath:db/changelog/db.changelog-master.yaml
    enabled: true

app:
  cors:
    allowed-origins: "http://localhost:5173"

jwt:
  secret: ${JWT_SECRET:9s4I1yPPE68IbnyW+xEgzISW2ospjKgWmkpPjDocykY=}
  expiration: 86400000
